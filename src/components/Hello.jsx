import React from 'react';


export default function Hello(props){
   
    return <div>
        这是 function 构造函数创建的 组件 -- {props.name} -- {props.name} -- {props.gender}
    </div>
}

//export default Hello